# Sitemap Index — MIABOS

`Design/Sitemap/` là canonical layer cho route/surface map của quy trình mới `Features -> UX/UI by Screen`.

Mỗi module/capability được break UXUI theo screen phải có:

- một `Sitemap`
- một `Flow Matrix`
- các `UXUI Screen Specs` liên kết ngược về hai artifact trên

## Registered Items

Chưa có canonical `Sitemap` active trong project. Pilot `M01 Product Query` đã bị gỡ vì không bám front-end source of truth.
